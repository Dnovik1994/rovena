# Logging to stdout (captured by Docker json-file driver)
log /dev/stdout

nscache 65536
timeouts 1 5 30 60 180 1800 15 60
maxconn 100

# Allow connections only from Docker internal networks
allow * 172.16.0.0/12
allow * 10.0.0.0/8
allow * 192.168.0.0/16
deny *

socks -p1080
